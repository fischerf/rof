"""
bot_service/routers
===================
FastAPI router modules for the ROF Bot REST API.

    control.py  — /control/* lifecycle endpoints (start, stop, pause, reload, force-run, emergency-stop)
    status.py   — /status, /runs, /runs/{run_id} read-only endpoints
    config.py   — /config read/write endpoints
"""
